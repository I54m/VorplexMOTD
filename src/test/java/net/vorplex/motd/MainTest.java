package net.vorplex.motd;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MainTest {

    @Test
    void onEnable() {
        assertTrue(true); //no tests to run as of yet, this is just to keep the jenkins build server happy
    }
}